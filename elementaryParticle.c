
// Find the biggest harmonius pair of subsegments in a k sized sequence with l(lower bound) r = higher bound 
// Two sub segments are different if l1 != l2 or r1 != r2
// 2 harmoinius pair are harmonius if for th i-th element the 2 subsegments has the same value


// Goal find the size of the biggest pair of subsegments



int main (){


	return 0;
}


int biggestHarSeg(int arr[]){
	
	int l1,l2, r1, r2;

	l1 = 0;
	l2 = 0;
	r1 = l1 + 1;
	r2 = l2 + 1;
	
	
}
